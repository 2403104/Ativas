<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Card</title>
    <style>

        .product-container {
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
            justify-content: center;
            width: 100vw;
        }

        .row {
            display: flex;
            justify-content: space-between;
            gap: 5px;
            width: 100%;
        }

        .product-card {
            display: flex;
            flex-direction: row;
            justify-content: space-between;
            align-items: center;
            width: 33%;
            padding: 20px;
            background: linear-gradient(90deg, #d9f5d1, #b2e7a3);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .product-info {
            max-width: 300px;
        }

        .product-info h2 {
            font-size: 24px;
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
        }

        .product-info p {
            font-size: 16px;
            color: #666;
            margin: 5px 0;
        }

        .product-info .price {
            font-size: 20px;
            font-weight: bold;
            color: #007600;
            margin-top: 10px;
        }

        .product-image {
            width: 150px;
            height: auto;
            object-fit: contain;
        }

        .product-image img {
            width: 100%;
            border-radius: 8px;
        }
    </style>
</head>
<body>
    <div class="product-container">
        <% for (let i = 0; i < LaptopAndTablet.length; i += 3) { %>
        <div class="row">
            <% for (let j = i; j < i + 3 && j < LaptopAndTablet.length; j++) { %>
                <div class="product-card" onclick="goToLaptopAndTablet('<%=LaptopAndTablet[j].brand%>','<%=LaptopAndTablet[j].category%>')">
                    <div class="product-info">
                        <h2>Best <%=LaptopAndTablet[j].brand%> <%= LaptopAndTablet[j].category %></h2>
                        <p>Prepaids | Supercoins</p>
                        <p class="price">₹<%= LaptopAndTablet[j].price.discountedPrice %></p>
                    </div>
                    <div class="product-image">
                        <img src="<%= LaptopAndTablet[j].images[0] %>" alt="<%= LaptopAndTablet[j].name %>">
                        <p style="text-align: center;"><%= LaptopAndTablet[j].brand %></p>
                    </div>
                </div>
            <% } %>
        </div>
        <% } %>
    </div>
    <script>
        function goToLaptopAndTablet(brand,category){
            window.location.href=`/productType?brand=${encodeURIComponent(brand)}&category=${encodeURIComponent(category)}`;
        }
    </script>
</body>
</html>
