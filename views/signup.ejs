<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ativas E-commerce Signup</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            height: 100vh;
            justify-content: center;
            align-items: center;
            background: linear-gradient(to right, #ffffff 50%, #007bff 50%);
        }

        .form-section {
            width: 50%;
            padding: 40px;
            background: #fff;
            text-align: center;
        }

        h1 {
            font-size: 2.5em;
            color: #333;
            margin-bottom: 20px;
        }

        form input {
            width: 90%;
            padding: 12px 15px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 25px;
            outline: none;
            font-size: 1em;
        }

        form input:focus {
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
        }

        .signup-button {
            width: 94%;
            padding: 12px 15px;
            margin: 20px 0;
            background: #007bff;
            color: #fff;
            font-size: 1.2em;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .signup-button:hover {
            background: #0056b3;
        }

        .right-section {
            width: 50%;
            padding: 50px;
            color: #fff;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .right-section h2 {
            font-size: 2.5em;
            margin-bottom: 20px;
        }

        .right-section p {
            font-size: 1.2em;
            margin-bottom: 20px;
        }

        .signin-button {
            padding: 10px 20px;
            color: #007bff;
            background: #fff;
            border-radius: 25px;
            text-decoration: none;
            font-size: 1em;
            font-weight: bold;
            transition: background 0.3s, color 0.3s;
        }

        .signin-button:hover {
            background: #007bff;
            color: #fff;
        }

        @media (max-width: 768px) {
            .form-section, .right-section {
                width: 100%;
                padding: 20px;
            }

            .signup-button {
                width: 100%;
            }

            .signin-button {
                width: 100%;
            }

            h1 {
                font-size: 2em;
            }

            .right-section h2 {
                font-size: 2em;
            }

            .right-section p {
                font-size: 1em;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="form-section">
            <h1>Sign up</h1>
            <form action="/auth/signup" method="post">
                <input type="text" id="fullName" placeholder="Full Name" name="fullName" required />
                <input type="text" id="mobileNumber" placeholder="Mobile Number" name="mobileNumber" required />
                <input type="email" id="email" placeholder="Email" name="email" required />
                <input type="password" id="password" placeholder="Password" name="password" required />
                <input type="password" id="confirmPassword" placeholder="Confirm Password" name="confirmPassword" required />
                <button type="submit" class="signup-button">Sign Up</button>
            </form>
        </div>

        <div class="right-section">
            <h2>One of us?</h2>
            <p>If you already have an account, sign in here!</p>
            <a href="/auth/login" class="signin-button">Sign In</a>
        </div>
    </div>
</body>

</html>
